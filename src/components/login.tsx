'use client';
import Link from 'next/link';
import { useState } from 'react';
import { ArrowUpRight, ArrowLeft, Eye, EyeOff, LoaderCircle, RadioTower } from 'lucide-react';
export default function Login(){const [passwordVisible,setPasswordVisible]=useState(false);const [busy,setBusy]=useState(false);const [error,setError]=useState('');async function login(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError('');const form=new FormData(e.currentTarget);try{const res=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:form.get('email'),password:form.get('password')})});const body=await res.json();if(!res.ok)throw new Error(body.error||'No se pudo iniciar sesión.');window.location.assign('/app');}catch(e){setError(e instanceof Error?e.message:'No se pudo conectar. Intentá nuevamente.');setBusy(false);}}return <main className="login-page"><div className="login-story"><Link className="brand" href="/">TNW<span>.</span><small>TIME NETWORK</small></Link><div className="login-story-main"><div className="eyebrow light"><span className="live-dot"/> OUTDOOR. UNDER CONTROL.</div><h1>Tu red.<br/>Toda la<br/><em>perspectiva.</em></h1><p>De la primera pantalla a la próxima ciudad.<br/>Un solo lugar para hacer que pase.</p><div className="login-orbit"><RadioTower size={62} strokeWidth={1}/><span className="orbit-dot one"/><span className="orbit-dot two"/><span className="orbit-dot three"/></div></div><span className="login-footer">TECNOLOGÍA QUE CONECTA LA VÍA PÚBLICA.</span></div><section className="login-panel"><Link className="back-link" href="/"><ArrowLeft size={16}/>Volver al inicio</Link><div className="login-form"><span className="eyebrow">BIENVENIDO A LA RED</span><h2>Todo empieza acá.</h2><p>Ingresá al centro de control de TNW.</p><form onSubmit={login}><label>Email<input name="email" type="email" autoComplete="username" required placeholder="tu@empresa.com"/></label><label>Contraseña<div className="password-input"><input name="password" type={passwordVisible?'text':'password'} autoComplete="current-password" required placeholder="Tu contraseña"/><button type="button" aria-label={passwordVisible?'Ocultar contraseña':'Mostrar contraseña'} onClick={()=>setPasswordVisible(!passwordVisible)}>{passwordVisible?<EyeOff size={19}/>:<Eye size={19}/>}</button></div></label>{error&&<div className="error-box" role="alert">{error}</div>}<button className="button primary" disabled={busy}>{busy?<LoaderCircle size={19} className="spin"/>:<>Ingresar al centro de control<ArrowUpRight size={19}/></>}</button></form><div className="login-demo"><span>¿Querés conocer la plataforma?</span><Link href="/demo">Explorar la demo interactiva<ArrowUpRight size={16}/></Link><small>Sin cuenta · Datos ilustrativos · Cambios locales</small></div><p className="login-access">El acceso se habilita por el administrador de tu organización. Si necesitás recuperar tu contraseña, contactalo para que la restablezca.</p></div><div className="login-panel-footer"><span>TNW © {new Date().getFullYear()}</span><a href="/manual-tnw.pdf" target="_blank" rel="noopener">Manual de uso ↗</a></div></section></main>;}
